<view class="mask" catchtouchmove="preventTouchMove" wx:if="{{showModal}}"></view>
<view class="modalDlg" wx:if="{{showModal}}">
  <block wx:for="{{list}}" wx:key="{{item.id}}">
    <text id="{{item.id}}" bindtap="go">{{item.title}}</text>
  </block>
  <!-- <text bindtap="go">{{list.title}}</text> -->
  <text>欢迎来到模态对话框~</text>
  <!-- <button bindtap="go">点我可以关掉对话框</button> -->
</view>
<button bindtap="submit">点我弹窗</button>
<!-- <view>
  <block wx:for="{{list}}" wx:key="{{item.id}}"> -->
<view id="{{item.id}}">{{moren}}</view>
<!-- </block>
</view> -->

 





<swiper autoplay="{{true}}" interval="1000" duration="500" vertical="{{true}}">
  <block wx:for="{{problemlunbo}}">
  <swiper-item style="height:100rpx">
    <text>{{item.Problem}}</text>
  </swiper-item>
  </block>
</swiper>


<block wx:for="{{problemlunbo}}">
    <text >
        {{item.Problem}}
    </text>
</block>